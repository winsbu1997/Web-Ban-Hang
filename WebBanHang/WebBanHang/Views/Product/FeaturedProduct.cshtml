@model IEnumerable<WebBanHang.Models.ProductModel>

@foreach (var item in Model)
{
    var url = "/chi-tiet/" + item.MetaTitle + "-" + item.ID;
    <div class="item item-carousel">
        <div class="products">
            <div class="product">
                <div class="product-image">
                    <div class="image"> <a href="@url"><img src=@item.Image alt=""></a> </div>
                    <!-- /.image -->
                    @if (item.Sale.HasValue)
                    {
                        <div class="tag sale"><span>sale</span></div>
                    }                    
                    <div class="tag hot"><span>hot</span></div>
                </div>
                <!-- /.product-image -->

                <div class="product-info text-left">
                    <h3 class="name"><a href="@url">@item.Name</a></h3>
                    <div class="rating rateit-small"></div>
                    <div class="description">@(item.Quantity.Value > 3 ? "Còn Hàng" : "Hết Hàng")</div>
                    <div class="product-price">
                        @if (item.Price.HasValue)
                        {
                            if (item.Sale.HasValue)
                            {
                                <span class="price">@((item.Price - (item.Price * item.Sale / 100)).Value.ToString("N0") + "VND")</span>
                                                <span class="price-before-discount">@(item.Price.Value.ToString("N0") + "VND")</span>
                            }
                            else
                            {
                                <span class="price"> @(item.Price.Value.ToString("N0") + "VND") </span>
                            }
                        }
                        else
                        {
                            <span class="price">Liên Hệ</span>
                        }
                    </div>
                    <!-- /.product-price -->

                </div>

                <!-- /.product-info -->
                <div class="cart clearfix animate-effect">
                    <div class="action">
                        <ul class="list-unstyled">
                            <li class="add-cart-button btn-group">
                                <button class="btn btn-primary icon" data-toggle="dropdown" type="button" onclick="toro(@item.ID)"> <i class="fa fa-shopping-cart"></i> </button>
                                <button class="btn btn-primary cart-btn" type="button">Add to cart</button>
                            </li>
                            @*<li class="lnk wishlist"> <a class="add-to-cart" href="@url" title="Wishlist"> <i class="icon fa fa-heart"></i> </a> </li>
                            <li class="lnk"> <a class="add-to-cart" href="@url" title="Compare"> <i class="fa fa-signal" aria-hidden="true"></i> </a> </li>*@
                        </ul>
                    </div>
                    <!-- /.action -->
                </div>
                <!-- /.cart -->
            </div>
            <!-- /.product -->

        </div>
        <!-- /.products -->
    </div>
    <!-- /.item -->
}

